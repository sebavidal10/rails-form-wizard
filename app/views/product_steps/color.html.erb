<%= form_with(model: @product, url: wizard_path, local: true) do |f| %>
  <%= f.label :color %>
  <%= f.text_field :color %>
  <br/>
  <%= link_to 'Back', previous_wizard_path %>
  <%= f.submit 'End' %>
<% end %>

